# Test Fixtures

The files in this directory are deterministic test fixtures only.

`fixture_priv.bin` is the fixed byte sequence `00 01 ... 1f`, and the
`kcap*.bin` files are capsules generated for compatibility tests. Do not use
any fixture from this directory as a deployment key or production capsule.
